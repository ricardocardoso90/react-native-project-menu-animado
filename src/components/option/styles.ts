import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: 42,
    padding: 12,
    borderRadius: 7,
    backgroundColor: '#F2F2F2',
    flexDirection: 'row',
    gap: 12,
    overflow: 'hidden'
  },
  title: {
    fontSize: 14,
    fontWeight: '600'
  }
});